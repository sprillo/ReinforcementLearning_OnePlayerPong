# ReinforcementLearning_OnePlayerPong
A simple test of the BURLAP framework to teach an agent to play (a one player version of) pong

Build with

```
mvn compile
```

Run with

```
mvn exec:java -Dexec.mainClass="myProj.PongMain"
```